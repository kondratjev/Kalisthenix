import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    color: "#fff",
    fontSize: 16,
    fontWeight: "600",
    marginBottom: 25,
  },
});

export default styles;
