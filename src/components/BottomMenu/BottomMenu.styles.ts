import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    fontWeight: "500",
    marginBottom: 15,
    paddingHorizontal: 25,
    paddingTop: 25,
    color: "#fff",
  },
  button: { paddingHorizontal: 25, paddingVertical: 15 },
  buttonLabel: { fontSize: 16, color: "#fff" },
});

export default styles;
